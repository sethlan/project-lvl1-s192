/*
import { car } from 'hexlet-pairs';
import games from '..';

const isbalanced = (number) => {
  const iter = (num, big, small) => {
    const remainder = num % 10;
    if (remainder >= small) {
      if (remainder >= big) {

      }
    }
  };
};
export default () => games(

)
*/
